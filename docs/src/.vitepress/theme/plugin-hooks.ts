// .vitepress/theme/plugin-hooks.ts
//
// Helpers injected by `Documenter.Plugin`s via `vitepress_theme_transform` (see
// `extension_hooks.jl`), kept out of `index.ts`. Empty by default.

// __DV_PLUGIN_THEME_HELPERS__

// Called once per activated <script> tag, from `index.ts`'s `activateScripts`.
export async function runPluginScriptHooks(): Promise<void> {
  // __DV_PLUGIN_SCRIPT_HOOK__
}
